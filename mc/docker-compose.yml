version: '3.2'

services:
  mc:
    container_name: "mc-el"
    image: itzg/minecraft-server
    environment:
      EULA: "true"
      MAX_MEMORY: 4G
      TYPE: CURSEFORGE
      CF_SERVER_MOD: "modpack.zip"
    volumes:
      - ./data:/data
    ports:
      - 25565:25565
